<UserControl x:Class="ChatApp.Views.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ChatApp.Views"
             xmlns:vm="clr-namespace:ChatApp.ViewModels" xmlns:components="clr-namespace:ChatApp.Components"
             d:DataContext="{d:DesignInstance Type=vm:ChatViewModel}"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"            
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Border Margin="10" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*"/>
                <RowDefinition />
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" FontSize="40" Text="Chat Page" HorizontalAlignment="Center"/>


            <materialDesign:Card Grid.Row="1">
                <ListBox x:Name="MessagesList" 
                         ItemsSource="{Binding Messages}"
                         Style="{StaticResource MaterialDesignListBox}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="5"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </materialDesign:Card>


            <StackPanel Orientation="Horizontal"
                        Margin="5" HorizontalAlignment="Right"
                        Grid.Row="2">
                <TextBox x:Name="UserInput" Width="600" 
                         Height="120" Margin="0,0,5,0" 
                         Padding="10 10 5 5" TextWrapping="Wrap"
                         Text="{Binding UserMessage, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource MaterialDesignFilledTextBox}"
                         materialDesign:TransitionAssist.DisableTransitions="True"
                         materialDesign:TextFieldAssist.DecorationVisibility="Hidden"
                         materialDesign:TextFieldAssist.TextFieldCornerRadius="15"
                         materialDesign:TextFieldAssist.UnderlineBrush="Black"
                         materialDesign:TextFieldAssist.TextBoxViewMargin="0 0 10 0"
                         CaretBrush="Black"
                         SelectionBrush="DodgerBlue"
                         VerticalContentAlignment="Top"
                         FontSize="{StaticResource FontSizeSmall}"
                         VerticalScrollBarVisibility="Auto"
                         AcceptsReturn="True"/>
                <Button Content="Send" x:Name="SendButton" 
                        Width="80" Height="60" Command="{Binding SendMessageCommand}" 
                        Margin="20,0,0,0"/>

            </StackPanel>
            <Button Grid.Row="3" Name="Navigate"
                    Padding="0"
                    Width="120" Height="40" Content="Account Home" 
                    Command="{Binding NavigateAccountCommand}"/>
        </Grid>
    </Border>
</UserControl>

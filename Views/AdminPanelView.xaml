<UserControl x:Class="ChatApp.Views.AdminPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ChatApp.Views"
             xmlns:vm="clr-namespace:ChatApp.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:AdminPanelViewModel}"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="boolToVis"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Admin Panel" HorizontalAlignment="Center" VerticalAlignment="Top"
                       FontSize="40"/>


        <materialDesign:Card Grid.Row="1">
            <DataGrid ItemsSource="{Binding Users}"
                      SelectedItem="{Binding SelectedUser}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      Margin="5">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="SizeToCells"/>
                    <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
                    <DataGridTextColumn Header="First" Binding="{Binding FirstName}" Width="*"/>
                    <DataGridTextColumn Header="Last" Binding="{Binding LastName}" Width="*"/>
                    <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="*"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                    <DataGridTextColumn Header="Mobile" Binding="{Binding MobileNumber}" Width="*"/>
                    <DataGridTextColumn Header="Verified" Binding="{Binding IsEmailVerified}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>

        <TextBox Grid.Row="2"
                 Background="Transparent"
                 materialDesign:HintAssist.Hint=""
                 BorderThickness="0"
                 IsReadOnly="True"
                 MaxHeight="40"
                 Margin="12 0 12 5"
                 VerticalScrollBarVisibility="Auto"
                 Text="{Binding ErrorMessage, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 Visibility="{Binding ShowErrorMessage, Converter={StaticResource boolToVis}}"
                 TextWrapping="Wrap"
                 HorizontalAlignment="Center"
                 FontSize="{StaticResource FontSizeSmall}"
                 Foreground="Red"/>

        <Grid Grid.Row="3">



            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center">
                <Button 
                    Content="Refresh"
                    Padding="0"
                    Margin="5"
                    Width="70"
                    Height="30"
                    Command="{Binding RefreshCommand}"/>
                <Button
                    Content="Edit"
                    Margin="5" 
                    Width="70"
                    Height="30"
                    Command="{Binding RefreshCommand}"/>
                <Button
                    Content="Remove"
                    Padding="0"
                    Margin="5"        
                    Width="70"
                    Height="30"
                    Command="{Binding DeleteSelectedCommand}"/>
            </StackPanel>

        </Grid>
    </Grid>
</UserControl>

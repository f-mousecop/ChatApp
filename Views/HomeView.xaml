<UserControl x:Class="ChatApp.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ChatApp.Views"
             xmlns:vm="clr-namespace:ChatApp.ViewModels"
             xmlns:components="clr-namespace:ChatApp.Components"
             d:DataContext="{d:DesignInstance Type=vm:HomeViewModel, IsDesignTimeCreatable=True}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="{DynamicResource ForegroundLightBrush}" CornerRadius="{StaticResource WindowCornerRadius}">


        <Grid Grid.Row="1" MaxWidth="1000" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" FontSize="28" Text="Account" HorizontalAlignment="Center"/>

            <StackPanel Grid.Row="1" Margin="5" HorizontalAlignment="Center">
                <TextBlock Margin="5" FontSize="{StaticResource FontSizeLarge}" Text="{Binding CurrentUserAccount.DisplayName}"/>
                <TextBlock Margin="5" FontSize="{StaticResource FontSizeLarge}" Text="{Binding CurrentUserAccount.Email}"/>
                <TextBlock Margin="5" FontSize="{StaticResource FontSizeRegular}" Name="curr_Date_Time" />
            </StackPanel>
            <Button Grid.Row="2" HorizontalAlignment="Center" 
                    Style="{StaticResource regularButtons}"
                    Width="100"
                    Height="50"
                    Command="{Binding NavigateLoginCommand}"
                    Content="Login"/>
        </Grid>

    </Border>
</UserControl>

<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                    xmlns:utils ="clr-namespace:ChatApp.Utils">

    <Style x:Key="AppSnackbar" TargetType="materialDesign:Snackbar" BasedOn="{StaticResource {x:Type materialDesign:Snackbar}}">
        <Setter Property="Background" Value="{StaticResource MaterialDesignSnackbarBackground}"/>
        <Setter Property="MaxWidth" Value="{Binding ActualWidth, ElementName=AppGrid}"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="VerticalAlignment" Value="Bottom"/>
        <Setter Property="Height" Value="80"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding Source={x:Static utils:SnackbarManager.Instance}, Path=CurrentMessageLevel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Value="1">
                <Setter Property="Background" Value="{StaticResource FwBrushError}"/>
                <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding Source={x:Static utils:SnackbarManager.Instance}, Path=CurrentMessageLevel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Value="2">
                <Setter Property="Background" Value="{StaticResource FwBrushWarning}"/>
                <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding Source={x:Static utils:SnackbarManager.Instance}, Path=CurrentMessageLevel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Value="3">
                <Setter Property="Background" Value="{StaticResource FwBrushInfo}"/>
                <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding Source={x:Static utils:SnackbarManager.Instance}, Path=CurrentMessageLevel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Value="4">
                <Setter Property="Background" Value="{StaticResource FwBrushSuccess}"/>
                <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="materialDesign:SnackbarMessage" BasedOn="{StaticResource {x:Type materialDesign:SnackbarMessage}}">
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Height" Value="60"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="materialDesign:SnackbarMessage">
                    <Grid>
                        <ContentPresenter Content="{TemplateBinding Content}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}"
                                          ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                          ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                          VerticalAlignment="{TemplateBinding VerticalAlignment}">
                        </ContentPresenter>
                        <Button x:Name="PART_ActionButton"
                                Visibility="Visible"
                                Width="50" Height="40"
                                Padding="0"
                                ContentTemplate="{TemplateBinding ActionContentTemplate}"
                                Content="{TemplateBinding ActionContent}"
                                HorizontalAlignment="Right"
                                VerticalAlignment="{TemplateBinding VerticalAlignment}"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>